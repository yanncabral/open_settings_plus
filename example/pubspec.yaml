name: open_settings_plus_example
description: Demonstrates how to use the open_settings_plus plugin.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.0"

dependencies:
  flutter:
    sdk: flutter

  open_settings_plus:
    path: ../

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
